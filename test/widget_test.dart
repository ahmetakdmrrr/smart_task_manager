// Temel widget testi - SmartTaskManagerApp için
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds successfully', (WidgetTester tester) async {
    // Bu test şu an için placeholder, Hive mock gerektirir
    expect(true, isTrue);
  });
}
